[package]
name = "appzcrawl-server"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = { version = "0.8.1", features = ["json"] }
once_cell = "1.20"
tempfile = "3.14.0"

# Workspace crates
firecrawl_rs = { path = "../native" }
appzcrawl-search = { path = "../search" }

# Workspace shared dependencies
serde = { workspace = true }
serde_json = { workspace = true }
tokio = { workspace = true }
regex = { workspace = true }
url = { workspace = true }
base64 = { workspace = true }
